namespace AirportSystem.Models
{
    public enum FlightStatus { Scheduled, Delayed, Cancelled, Boarding, Departed }
    public enum TicketClass { Economy, Business, First }
    public enum TicketStatus { Paid, Registered, Cancelled }
}